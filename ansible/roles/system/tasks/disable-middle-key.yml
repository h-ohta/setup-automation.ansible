---

- name: disable-middle-key task
  tags: disable-middle-key
  block:
  - name: disable-middle-key, install
    apt:
      name:
        - xbindkeys
        - xsel
        - xdotool
      state: latest
      update_cache: yes
    become: yes


  - name: disable-middle-key, file copy
    copy:
      src: "{{ item }}"
      dest: ~/
    with_fileglob:
      - ../files/.xbindkeysrc

  - name: disable-middle-key, auto apply
    lineinfile:
      dest: ~/.xsessionrc
      line: xbindkeys -p
      state: present
      create: yes
